<UserControl x:Class="TFOHelperRedux.Views.CatchPointEditorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:TFOHelperRedux.Services"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400">

    <Border Margin="2"
            Background="{DynamicResource PanelBackground}"
            CornerRadius="6"
            BorderBrush="{DynamicResource BorderBrushLight}"
            Padding="16">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,10,0">
            <Separator Margin="0,10,0,10" Background="{DynamicResource BorderBrushLight}"/>
            <!-- Водоём -->
            <TextBlock Text="Водоём:" FontWeight="Bold" Margin="0,0,0,4" Foreground="{DynamicResource TextPrimary}"/>
            <ComboBox x:Name="cmbMap"
                      Height="30"
                      DisplayMemberPath="Name"/>
            <!-- Координаты -->
            <TextBlock Text="Координаты:" FontWeight="Bold" Margin="0,10,0,4" Foreground="{DynamicResource TextPrimary}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <TextBox x:Name="tbX" Width="70" HorizontalContentAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource TextPrimary}"/>
                <TextBox x:Name="tbY" Width="70" HorizontalContentAlignment="Center" Foreground="{DynamicResource TextPrimary}"/>
            </StackPanel>

                <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                    <StackPanel Width="120">
                        <TextBlock
                            Margin="0,0,0,4"
                            FontWeight="Bold"
                            Text="Крупная от (г)" />
                        <TextBox Text="{Binding SelectedLargeText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="35" HorizontalContentAlignment="Center" Margin="0, 0, 0, 0" Width="80" Foreground="{DynamicResource TextPrimary}"/>
                    </StackPanel>
                    <StackPanel Width="120" Margin="16,0,0,0">
                        <TextBlock
                            Margin="0,0,0,4"
                            FontWeight="Bold"
                            Text="Трофей от (г)" />
                        <TextBox Text="{Binding SelectedTrophyText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="35" HorizontalContentAlignment="Center" Margin="0, 0, 0, 0" Width="80" Foreground="{DynamicResource TextPrimary}"/>
                    </StackPanel>
                </StackPanel>

            <!-- Глубина + клипса -->
            <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                <StackPanel Width="120">
                    <TextBlock
                        Margin="0,0,0,4"
                        FontWeight="Bold"
                        Text="Глубина:"
                        Foreground="{DynamicResource TextPrimary}"/>
                    <TextBox x:Name="tbDepth"
                             Width="80"
                             HorizontalContentAlignment="Center"
                             Margin="0, 0, 0, 4"
                             Foreground="{DynamicResource TextPrimary}"/>
                </StackPanel>
                <StackPanel Width="120" Margin="16,0,0,0">
                    <TextBlock
                        Margin="0,0,0,4"
                        FontWeight="Bold"
                        Text="Клипса:"
                        Foreground="{DynamicResource TextPrimary}"/>
                    <TextBox x:Name="tbClip"
                             Width="80"
                             HorizontalContentAlignment="Center"
                             Margin="0, 0, 0, 4"
                             Foreground="{DynamicResource TextPrimary}"/>
                </StackPanel>
            </StackPanel>

            <!-- Особенности -->
            <TextBlock Text="Особенности:" FontWeight="Bold" Margin="0,10,0,4" Foreground="{DynamicResource TextPrimary}"/>
            <StackPanel Orientation="Vertical">
                <CheckBox x:Name="cbTrophy" Content="Трофейная"/>
                <CheckBox x:Name="cbTournament" Content="Турнирная"/>
                <CheckBox x:Name="cbCautious" Content="Осторожная"/>
            </StackPanel>
            <!-- Время лова -->
            <TextBlock Text="Время лова:" FontWeight="Bold" Margin="0,10,0,4" Foreground="{DynamicResource TextPrimary}"/>
            <Border BorderThickness="1" HorizontalAlignment="Left" BorderBrush="{DynamicResource BorderBrushLight}">
                <StackPanel Width="100" Orientation="Vertical">
                    <CheckBox x:Name="cbMorning" Content="Утро 🌅"/>
                    <CheckBox x:Name="cbDay" Content="День ☀️"/>
                    <CheckBox x:Name="cbEvening" Content="Вечер 🌇"/>
                    <CheckBox x:Name="cbNight" Content="Ночь 🌙"/>
                </StackPanel>
            </Border>
            <!-- Тип удилища -->
            <TextBlock Text="Тип удилища:" FontWeight="Bold" Margin="0,10,0,4" Foreground="{DynamicResource TextPrimary}"/>
            <StackPanel>
                <CheckBox x:Name="cbRodSpinning" Content="Спиннинг"/>
                <CheckBox x:Name="cbRodFeeder" Content="Фидер"/>
                <CheckBox x:Name="cbRodFloat" Content="Поплавок"/>
                <CheckBox x:Name="cbRodFly" Content="Нахлыст"/>
                <CheckBox x:Name="cbRodSea" Content="Морское"/>
            </StackPanel>
            <!-- Комментарий -->
            <TextBlock Text="Комментарий:" FontWeight="Bold" Margin="0,10,0,4" Foreground="{DynamicResource TextPrimary}"/>
            <Border Background="{DynamicResource PanelBackground}" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1" CornerRadius="4">
                <TextBox x:Name="tbComment"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         Padding="6"
                         Background="Transparent"
                         VerticalScrollBarVisibility="Auto"
                         Height="140"
                         Foreground="{DynamicResource TextPrimary}"/>
            </Border>
        </StackPanel>
    </ScrollViewer>
    </Border>

</UserControl>