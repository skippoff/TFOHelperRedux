<UserControl x:Class="TFOHelperRedux.Views.FishView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TFOHelperRedux.ViewModels"
             xmlns:conv="clr-namespace:TFOHelperRedux.Converters"
             xmlns:local="clr-namespace:TFOHelperRedux.Views"
             xmlns:svc="clr-namespace:TFOHelperRedux.Services"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:TFOHelperRedux.Views"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <!--MinHeight="600" MinWidth="900"-->
    <UserControl.DataContext>
        <vm:FishViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <conv:EqualityToVisibilityConverter x:Key="EqualityToVisibilityConverter"/>
        <conv:BiteToHeightConverter x:Key="BiteToHeightConverter"/>
        <conv:StringToImageSourceConverter x:Key="ImgConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <DropShadowEffect x:Key="DropShadow" BlurRadius="10" ShadowDepth="2" Opacity="0.25" />
        <!-- Стиль кнопки-иконки -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PanelBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushLight}"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Стиль для ListBoxItem категорий -->
        <Style x:Key="CategoryListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2196F3"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#BBDEFB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Стиль для карточек рыб -->
        <Style x:Key="FishCardStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderBrushLight}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="8">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="{StaticResource AppBackground}"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Header / Toolbar -->
        <Border Grid.Row="0"
        Background="{StaticResource PanelBackground}"
        BorderBrush="{StaticResource BorderBrushLight}"
        BorderThickness="0,0,0,0"
        Padding="0,0">
            <Border Grid.Row="0" 
        Background="#ff708090"
        BorderBrush="{StaticResource BorderBrushLight}" 
        BorderThickness="0,0,0,1"
        Padding="8,6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- 📁 Импорт / Экспорт / Очистить -->
                    <views:ActionPanel Grid.Column="0" Grid.Row="0"/>
                    <!-- 🧭 Навигация -->
                    <views:NavigationPanel Grid.Column="0" Grid.Row="1" />
                </Grid>
            </Border>
        </Border>
        <!-- Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="575"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- LEFT: Категории -->
            <Grid>
                <!-- Новая панель категорий -->
                <local:FishCategoriesPanel
                    Visibility="{Binding CurrentMode,
                                         Converter={StaticResource EqualityToVisibilityConverter},
                                         ConverterParameter=Fish}" />
                <!-- Прикорм и дипы -->
                <Border x:Name="BaitsPanel"
                        Margin="2"
                        Background="AliceBlue" 
                        CornerRadius="6" 
                        BorderBrush="#DDDDDD" 
                        BorderThickness="1" 
                        Padding="12"
                        Visibility="{Binding CurrentMode, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=Baits}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="ОСНАСТКА"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       Margin="4,0,0,8"
                                       Foreground="#666"/>
                            <ListBox SelectedValue="{Binding BaitsSubMode, Mode=TwoWay}"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     ItemContainerStyle="{StaticResource CategoryListBoxItem}"
                                     SelectedValuePath="Tag">
                                <ListBoxItem Content="Прикормки" Tag="Feeds"/>
                                <ListBoxItem Content="Дипы" Tag="Dips"/>
                                <ListBoxItem Content="Наживки" Tag="Lures"/>
                                <ListBoxItem Content="Компоненты прикормки" Tag="FeedComponents"/>
                                <ListBoxItem Content="Топовые наживки" Tag="CraftLures"/>
                            </ListBox>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
            <local:MapsPanel Grid.Column="0"
                             Grid.ColumnSpan="3"
                             Visibility="{Binding CurrentMode,
                Converter={StaticResource EqualityToVisibilityConverter},
                ConverterParameter=Maps}"/>
            <local:FishPanel Grid.Column="1"
                             Grid.ColumnSpan="2"
                             Visibility="{Binding CurrentMode,
                Converter={StaticResource EqualityToVisibilityConverter},
                ConverterParameter=Fish}"/>
            <local:BaitsPanel Grid.Column="1"
                              Grid.ColumnSpan="2"
                              Visibility="{Binding CurrentMode,
                Converter={StaticResource EqualityToVisibilityConverter},
                ConverterParameter=Baits}"/>
            <local:FishDetailsPanel Grid.Column="2">
                <local:FishDetailsPanel.Style>
                    <Style TargetType="local:FishDetailsPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <!-- Показывать панель деталей только при Maps и Fish -->
                            <DataTrigger Binding="{Binding CurrentMode}" Value="Maps">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding CurrentMode}" Value="Fish">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </local:FishDetailsPanel.Style>
            </local:FishDetailsPanel>
        </Grid>
    </Grid>
</UserControl>