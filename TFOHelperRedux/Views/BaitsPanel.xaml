<UserControl x:Class="TFOHelperRedux.Views.BaitsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TFOHelperRedux.Views"
             xmlns:views="clr-namespace:TFOHelperRedux.Views"
             xmlns:viewModels="clr-namespace:TFOHelperRedux.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <!-- содержимое (прикормки/дипы/наживки/компоненты) -->
            <ColumnDefinition Width="*"/>
            <!-- рецепты -->
        </Grid.ColumnDefinitions>

        <!-- ====================== CONTENT PANEL ====================== -->
        <Border Grid.Column="0"
                Background="{StaticResource PanelBackground}"
                BorderBrush="{StaticResource BorderBrushLight}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12"
                Margin="2">
            <ListBox ItemsSource="{Binding BaitsVM.CurrentItems}"
                     SelectedItem="{Binding BaitsVM.SelectedItem, Mode=TwoWay}"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemContainerStyle="{StaticResource RoundedCardStyle}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.CanContentScroll="True"
                     ScrollViewer.IsDeferredScrollingEnabled="True"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     VirtualizingPanel.CacheLength="2"
                     VirtualizingPanel.CacheLengthUnit="Item"
                     MouseDoubleClick="Ingredient_MouseDoubleClick">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <!-- UniformGrid поддерживает виртуализацию в отличие от WrapPanel -->
                        <!-- 3 колонки: 380px ширина / ~120px на карточку = 3 колонки -->
                        <UniformGrid Columns="3" IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- Фиксированный размер для корректной виртуализации -->
                        <Border Width="100" Height="100" Margin="4"
                                BorderBrush="#DDD" BorderThickness="1"
                                CornerRadius="6" Padding="4">
                            <StackPanel>
                                <Image Source="{Binding ImagePath, Converter={StaticResource ImgConverter}}" 
                                       Height="50" 
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="LowQuality"/>
                                <TextBlock Text="{Binding Name}" 
                                           FontSize="13" 
                                           TextAlignment="Center" 
                                           Margin="0,4,0,0" 
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- ====================== RECIPE / CRAFT PANEL ====================== -->
        <Border Grid.Column="1"
                Background="{StaticResource PanelBackground}"
                BorderBrush="{StaticResource BorderBrushLight}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="2"
                Margin="2">
            <Grid>
                <!-- Обычные рецепты прикорма -->
                <local:BaitRecipesPanel DataContext="{Binding BaitRecipesVM}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
