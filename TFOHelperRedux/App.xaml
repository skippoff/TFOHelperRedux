<Application x:Class="TFOHelperRedux.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:TFOHelperRedux.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             ShutdownMode="OnMainWindowClose">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- 🎨 MaterialDesignThemes -->
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Blue" SecondaryColor="LightBlue"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>

                <!-- 🎨 Светлая тема (по умолчанию) -->
                <ResourceDictionary Source="Themes/LightColors.xaml"/>

                <!-- 🎨 Преобразование Color → Brush для совместимости -->
                <ResourceDictionary>
                    <SolidColorBrush x:Key="AppBackground" Color="{StaticResource AppBackgroundColor}"/>
                    <SolidColorBrush x:Key="PanelBackground" Color="{StaticResource PanelBackgroundColor}"/>
                    <SolidColorBrush x:Key="TextPrimary" Color="{StaticResource TextPrimaryColor}"/>
                    <SolidColorBrush x:Key="TextSecondary" Color="{StaticResource TextSecondaryColor}"/>
                    <SolidColorBrush x:Key="BorderBrushLight" Color="{StaticResource BorderBrushLightColor}"/>
                    <SolidColorBrush x:Key="PrimaryColor" Color="{StaticResource PrimaryColorValue}"/>
                    <SolidColorBrush x:Key="SuccessColor" Color="{StaticResource SuccessColorValue}"/>
                    <SolidColorBrush x:Key="DangerColor" Color="{StaticResource DangerColorValue}"/>
                    <SolidColorBrush x:Key="AccentLocation" Color="{StaticResource AccentLocationColor}"/>
                    <SolidColorBrush x:Key="AccentFeed" Color="{StaticResource AccentFeedColor}"/>
                    <SolidColorBrush x:Key="ButtonNavLocationBrush" Color="{StaticResource ButtonNavLocationColor}"/>
                    <SolidColorBrush x:Key="ButtonNavBaitBrush" Color="{StaticResource ButtonNavBaitColor}"/>
                </ResourceDictionary>

                <!-- 🎨 Локальная тема -->
                <ResourceDictionary Source="Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 🔄 Конвертеры -->
            <conv:StringToImageSourceConverter x:Key="ImgConverter"/>
            <conv:EqualityConverter x:Key="EqualityConverter"/>
            <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <conv:BiteToHeightConverter x:Key="BiteToHeightConverter"/>
            <conv:EqualityToVisibilityConverter x:Key="EqualityToVisibilityConverter"/>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <conv:RecipeRankToStyleConverter x:Key="RecipeRankToStyleConverter"/>
            <conv:RecipeCompositionConverter x:Key="RecipeCompositionConverter"/>
            <conv:MapGroupConverter x:Key="MapGroupConverter"/>

            <Style TargetType="{x:Type ScrollViewer}">
                <EventSetter Event="PreviewMouseWheel" Handler="ScrollViewer_PreviewMouseWheel"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
